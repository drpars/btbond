"""btbond — Bluetooth bond'larını host Linux ile misafir Windows arasında replike eder.

Paket **hiçbir alt modülü burada import etmiyor**, ve bu bilerek: `tui`
`textual` istiyor, `hivebond` `hivex` istiyor, ve ikisi de her koşunun
bağımlılığı değil. `import btbond` bu yüzden hiçbir şeyi zorunlu kılmıyor.
"""

__version__ = "0.1.0"
